# hero-motion 장면표 — 실측 확정판 (HEAD 5fd8ba1 기준)

- 원본 계획: `docs/task-id/hero-motion/plan.md` §2.2 설계값을 구현·V1 비전 검수까지 마친 확정표다.
- 문구 원문은 `docs/hero/js/config.js` 최종본과 일치한다(S5는 V1 GAP-2 갱신분).
- 실측치: 총 재생시간 24.00초 · 20fps(프레임당 50ms) · 480프레임 · GIF 960×540 · 7,596,082B(7.24MB) · 포스터 1920×1080(46,308B — t=23.95 스왑 완료 구도) · MP4 생략(ffmpeg 부재).

## 1. 장면표

| # | 시간대 | 지속 | 핵심 메시지 | 문구 원문(CONFIG 값) | 시각 요소·행동 | 전환/연결 | 코드 근거 |
|---|--------|------|-------------|----------------------|----------------|-----------|-----------|
| S0 | 0.00–2.60 | 2.6s | 제품 정체 + 한 줄 가치 | 제목 `Effort Router` / 부제 `작업 규모에 맞는 모델과 에포트를 배정한다` | t=0부터 제목·부제 완전 표시(페이드인 없음). 마커 언더라인 + 연필이 제목 스트로크를 다시 그리는 미세 모션 | 종이 배경은 전 장면 공통. 카드는 S1에서 화면에 진입 | `js/config.js:49` · `js/scenes.js:51` · README.md:1–3 |
| S1 | 2.60–6.20 | 3.6s | 단일 에포트의 실패 | `하나의 에포트로 전부 처리하면 —` / `단순한 일엔 과투자, 큰 일엔 과소검증` | 같은 크기 망치 3개가 크기 다른 카드 3장(라벨 `오타 수정`·`작은 버그`·`대형 리팩터링 XL`)을 차례 내리친다. 작은 카드엔 망치가 과하고 XL 카드엔 부족하다 | 작은 두 장은 낙하 퇴장, 주인공 XL 카드만 중앙으로 이동해 남는다. 점선 궤적이 S2 게이트로 이어진다 | `js/config.js:55` · `js/scenes.js:110` · README.md:9("왜 필요한가") |
| S2 | 6.20–9.80 | 3.6s | 티어 판정 | `규모·위험도를 보고` / `S / M / L / XL 로 판정한다` | 카드가 아치형 판정 게이트를 통과하며 스탬프 `XL`을 받는다. 게이트 옆 작은 배지 `jev` 1회 등장(확대 없음) | XL 자국이 카드에 남은 채 화면 좌측으로 퇴장, S3 레일 진입으로 이어진다 | `js/config.js:80` · `js/scenes.js:142` · SKILL.md:51(§1 티어 판정) · 배지: scripts/jev_judge.py · scripts/jev_modes.py |
| S3 | 9.80–15.40 | 5.6s | 단계마다 다른 역할·모델 | `단계마다 다른 모델과 역할 —` / `계획 → 적대 검토 → 구현 → 리뷰` | 손그림 레일 위 4개 정거장(라벨 `계획`·`적대 검토`·`구현`·`리뷰`). 카드가 레일을 따라 이동하며 정거장마다 색이 다른 펜·스탬프의 체크 도장이 카드에 누적된다. 이동할 때마다 점선 궤적이 길어진다 | 4번째 도장 후 카드가 오른쪽으로 미끄러지며 기울어짐 — S4 서류철 크로스페이드로 변형 완결 | `js/config.js:94` · `js/scenes-b.js:37` · SKILL.md:199(§2 라우팅 테이블) · 역할: agents/plan-high.md · agents/plan-adversary-xhigh.md · agents/implement-med.md · agents/implement-xhigh.md · agents/review-pr-high.md · agents/review-pr-xhigh.md |
| S4 | 15.40–19.60 | 4.2s | 상태 계약으로 인계 | `단계 사이는` / `state.json과 증거번들로 인계된다` | 카드가 서류철(표지 라벨 `state.json`)로 변형 완결, 영수증 슬립 3장이 하나씩 붙는다(행: `티어 판정 - XL`·`계획·검토 - 합의 완료`·`구현·리뷰 - 증거 13건`, 초록 체크). 그룹이 화면 중앙으로 정리된다 | S5 구도로 크로스페이드 | `js/config.js:115` · `js/scenes-b.js:102` · SKILL.md:311(§5 상태·인계 계약) · SKILL.md:323(증거번들) · SKILL.md:348(state.json 단일 writer) · 실례: docs/task-id/hero-motion/state.json |
| S5 | 19.60–24.00 | 4.4s | 완결과 반복(루프) | 제목 `Effort Router` / 요약줄 `티어 판정 → 계획 → 검토 → 구현 → 리뷰 — 다음 과업으로` → 부제가 `작업 규모에 맞는 모델과 에포트를 배정한다`(S0 태그라인)로 스왑 | 완성 서류철이 흐름선(초록 레일·노드 5개) 위에 착지. 요약줄이 온전히 읽힌 뒤(local 3.0s, 표시 2.8s ≥ 최소 가독 2.0s) 마지막 1.3s에 S5 전용 요소가 사라지고 태그라인이 등장 | 프레임 479 = 타이틀+밑줄+태그라인 = 프레임 0과 동일 구도(셀 해시 0/64 실측) — 무한 루프 단절 없음 | `js/config.js:130` · `js/scenes-b.js:139` · SKILL.md:51(§1) · SKILL.md:199(§2 라우팅 어휘 — V1 GAP-2로 근거를 README.md:6 흐름 인용에서 갱신) · 루프 개념: SKILL.md:337 |

보존 계약 준수: 이 표의 모든 문구 리터럴은 `js/config.js`에 상수로 존재하며, 렌더 파일(html·js·css)에는 문구 리터럴이 없다(grep 0건).

## 2. 코드 근거 경로 실존 확인

| 경로 | 역할 |
|------|------|
| README.md | 제품명·태그라인·왜 필요한가 |
| SKILL.md | 티어 판정·라우팅 테이블·상태·인계 계약·반복 루프 |
| scripts/jev_judge.py, scripts/jev_modes.py | S2 배지(jev) 근거 |
| agents/plan-high.md, agents/plan-adversary-xhigh.md | S3 정거장(계획·적대 검토) 근거 |
| agents/implement-med.md, agents/implement-xhigh.md | S3 정거장(구현) 근거 |
| agents/review-pr-high.md, agents/review-pr-xhigh.md | S3 정거장(리뷰) 근거 |
| docs/task-id/hero-motion/state.json | S4 state.json 실례 |
| docs/hero/js/config.js | 모든 문구·수치의 단일 출처 |

전 경로 `test -f` 실존(2026-09-26 확인 — 아래 실행 기록).

## 3. V1 비전 프레임 검수 결과 (claim 13c)

검수 주체: 메인 세션이 비전 가능 서브에이전트에 위임(구현자는 비전 없음). 대상: 각 씬 대표 6프레임 + 프레임 0/479 등 10프레임, 갱신 후 3프레임 재검수.

| 검수 항목 | 판정 | 방식 |
|-----------|------|------|
| 한글 렌더 깨짐(두부 문자) | 확인 — 결함 0 | 비전 프레임 검수 + `document.fonts.check` 오프라인 로그 |
| 텍스트 겹침 | 확인 — 결함 0 | export bbox 기계 검사(11표본 전수 0건) + 비전 검수 |
| 텍스트 잘림(캔버스 밖) | 확인 — 결함 0 | export bbox 기계 검사 + 비전 검수 |
| 루프 이음새 연속성 | 확인 — V1 GAP-1 수정 후 재검수 통과(00479↔00000 픽셀 diff 0.30%, 밑줄 지터만) | 비전 검수 + 셀 해시 0/64 |
| 축소 가독(README 표시) | 확인 | 비전 검수(본문 ≥48px → GIF 24px) |
| 문구-구현 일치 | 확인 — S5 갱신 문구(00436) 정확·S3 어휘 일치 | 비전 재검수(3프레임) |
| 폰트 활성(오프라인) | 확인 | `document.fonts.check('48px Gaegu') === true` 로그 |

### ④ GAP-R1 크로스페이드 창 재설계 후 비전 재검수 (2026-09-26, 메인 주관)

| 프레임(시점) | 결과 |
|--------------|------|
| 00050 (t=2.50, 창 내 합성) | 2씬 반투명 합성 깨끗 — 잔상·아티팩트 0 |
| 00052 (t=2.60, 경계) | 텍스트 스왑 완료·그래픽 0.5+0.5 합성 정상 |
| 00062 (t=3.10, S1) | S1 완성 선명 |
| 포스터(t=23.95) ↔ 프레임 0 | 동일 구도 — diff 0.30%(밑줄 지터만) |
| 경계 5프레임(00052·00124·00196·00308·00392) | 렌더 결함 0 |

### 미확인·잔여 항목 (완료로 둔갑시키지 않는다)

| 항목 | 상태 | 사유 |
|------|------|------|
| 00252(S3) 레일 3번 노드가 이동 중인 카드에 부분 가림 | 의도 보고로 종결 | 카드가 레일 위를 이동하며 정거장을 통과하는 구조상 필연. 정거장 간격 510px > 카드 폭 450px이라 정지 시 가림 없음 |
| 00160(S2 중반) 우측 ~55% 공백 | 부분 완화(배지 x 850→980) | S2 동선(S1 중앙→좌측 게이트→좌측 퇴장→S3 좌측 진입)의 일관성이 우선. 완전 해소는 동선 재설계 필요 — 범위 밖 |
| 실제 GitHub 페이지에서의 최종 렌더 확인 | 미확인 — 사용자 몫 | 로컬 산출과 상대경로 계약(GIF·포스터)만 보증. 사용자가 README 삽입 후 실제 페이지에서 확인 |
