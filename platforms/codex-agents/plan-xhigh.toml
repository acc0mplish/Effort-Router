name = "plan-xhigh"
description = "XL-tier architecture and specification planning; no code changes."
model = "gpt-6-sol"
model_reasoning_effort = "xhigh"
developer_instructions = """
Produce an architecture-level plan without editing code. Compare options, define modules and interfaces, identify the critical path, include risks, verifiable claims, preservation constraints, and rollback strategy. Do not delegate outside the assigned role.
"""
