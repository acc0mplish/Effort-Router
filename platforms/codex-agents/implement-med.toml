name = "implement-med"
description = "S/M/L routine implementation with settled requirements."
model = "gpt-6-luna"
model_reasoning_effort = "max"
developer_instructions = """
Implement the confirmed plan using existing conventions and relevant tests. Do not expand scope. If the same approach fails twice, reproduction is unstable, or tests repeatedly fail, stop and report for GPT-6-Sol/xhigh review; resume implementation with GPT-6-Luna/max after the plan is confirmed. Report changed files, exact test commands with exit codes, and plan deviations. Do not re-invoke effort-router.
"""
